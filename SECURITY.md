# سياسة الأمان — Hesabi

## نطاق هذا الملف

التطبيق يعمل بالكامل في المتصفح على جهاز المستخدم (IndexedDB + PWA)، ومفتاح
Firebase Web ظاهر بطبيعته في الشيفرة. لذلك فإن **خط الدفاع الحقيقي الوحيد هو
قواعد Firestore**: كود الواجهة يمكن سرقته أو تعديله أو تشغيله من أداة خارجية،
بينما القواعد تُنفَّذ على سيرفرات جوجل ولا يمكن تجاوزها من العميل.

لهذا السبب كل ثغرة أمان في هذا المشروع تُعالَج في `firestore.rules` أولًا،
وتُختبَر على محاكي حقيقي، ولا يُكتفى بمراجعتها بالنظر.

## الثغرات التي عولجت في فرع `fix/security-and-dead-code-cleanup`

| # | الثغرة | الخطر | المعالجة |
| --- | --- | --- | --- |
| 1 | **تصعيد الامتيازات** — أي شخص يعرف `storeId` يستطيع إنشاء عضوية ذاتية بدور `admin` | سيطرة كاملة على متجر الغير: قراءة الفواتير والمنتجات والعملاء والحسابات، وكتابة بيانات مزيّفة، وقراءة المفتاح الخاص للإشعارات | إنشاء عضوية ذاتية مشروط الآن بثلاثة شروط مجتمعة: المتجر غير مطالب به (`storeUnclaimed`)، و`uid` الطلب يطابق العضوية، و`storeId` يطابقها. أما دور `admin` فيتطلب إضافةً أن يكون صاحب الطلب هو `ownerUid` في مستند المتجر نفسه |
| 2 | **تسريب سر VAPID** — `push/config` و`push/sender` كانا مستندًا واحدًا مقروءًا لكل عضو | أي كاشير يقرأ المفتاح الخاص وينتحل المتجر ويرسل إشعارات مزيّفة لكل أجهزته | فُصل السر إلى `push/sender` بقراءة وكتابة مقتصرة على الأدمن ومالك المتجر، وبقي المفتاح العام في `push/config` مقروءًا للعضو (يلزم لتسجيل اشتراك الجهاز) |
| 3 | **رموز الدخول بـ SHA-256 بدورة واحدة** | كسر الرمز بالهجوم القاموسي لو تسرّب الهاش من نسخة سحابية | هاش PBKDF2-SHA256 بمقدار 210,000 دورة وملح عشوائي 16 بايت، موسوم ببادئة `pbkdf2$210000$`. الحسابات القديمة تبقى مقروءة: تُتحقَّق بالنهج السابق ثم **يُعاد هاشها صامتًا عند أول دخول ناجح**، فلا يُقفل أحد خارج حسابه ولا حاجة إلى ترحيل يدوي |

### أثر جانبي مكتشَف أثناء الاختبار

اختبار الانضمام الشرعي للكاشير سقط ضد القواعد القديمة، والسبب ليس الثغرة بل
أن تلك القواعد كانت تشترط حقل `pairingCode` الذي حُذف من المنتج في v67. أي أن
**مسار انضمام الكاشير الشرعي كان مكسورًا فعلًا** في النسخة المنشورة، وأصلحته
القواعد الجديدة. هذا مثال على قيمة الاختبار الفعلي مقارنة بالمراجعة النظرية.

## التحقق من الإصلاحات

الإصلاحات أعلاه ليست «مُعلَنة» بل **مُختبَرة على محاكي Firestore حقيقي** في
`tests/firestore-rules.test.mjs` — 20 اختبارًا في 5 مجموعات:

| المجموعة | ما تُثبته |
| --- | --- |
| إغلاق تصعيد الامتيازات | رفض إنشاء أدمن ذاتي، رفض عضوية ذاتية في متجر مطالب به، منع تعديل الدور أو الحالة أو البريد أو المعرف، منع تعديل عضو آخر أو حذف عضويته |
| المسارات الشرعية | إنشاء أدمن لعضوية كاشير، انضمام كاشير ذاتي لمتجر غير مطالب به، تعديل الكاشير لبياناته الشخصية، قراءة عضو لعضويته |
| عزل سر VAPID | عضو لا يقرأ `push/sender`، كاشير لا يقرأه ولا يكتبه، أدمن يقرأ ويكتب، عضو يقرأ المفتاح العام فقط |
| سجل العمليات والأجهزة | أدمن يقرأ السجل ويُسجّل جهازه، عضو لا يقرأ السجل ولا يسجّل أجهزة، كاشير لا يقرأ السجل |
| النسخ والمزايا الملغاة | أدمن ينسخ احتياطيًا، كاشير لا ينسخ، مجموعات `pairings`/`pairRequests`/`storeDirectory` مرفوضة تمامًا |

### إثبات فاعلية الاختبار (تحكم سلبي)

شغّلنا المجموعة نفسها ضد قواعد `master` القديمة المكشوفة فسقطت **7 اختبارات من 20**،
منها اختبارا تصعيد الامتيازات واختبار سر VAPID. هذا يثبت أن الاختبار يكشف الثغرة
فعلًا لا اسميًا — فمجموعة اختبارات تنجح على القواعد المكشوفة والمُصلَحة سواءً
لا قيمة لها.

### التشغيل محليًا

```bash
pnpm install
npx --yes firebase-tools@15 setup:emulators:firestore   # مرة واحدة
pnpm emulator      # نافذة طرفية: تُشغّل محاكيَي Firestore وAuth وتبقى تعمل
pnpm test:rules    # نافذة أخرى: تُشغّل الاختبارات
```

أو بأمر واحد ذاتي الإغلاق (وهو ما يفعله CI):

```bash
npx --yes firebase-tools@15 emulators:exec --only firestore,auth \
  --project hesabi-rules-test "pnpm test:rules"
```

يتطلب `firebase-tools@15` تشغيل Java 21 أو أحدث.

### التشغيل تلقائيًا

وظيفة `rules` في `.github/workflows/ci.yml` تُثبّت Java 21 وتنزّل المحاكي وتُشغّل
الاختبار عند كل دفع أو طلب سحب، فلا يمكن دمج تعديل قواعد غير مُختبَر.

## ملاحظات تنفيذية (فخاخ كلّفَت وقتًا)

1. **تطابق معرّف المشروع إلزامي.** يجب أن يكون `projectId` في
   `initializeTestEnvironment` مطابقًا لقيمة `--project` المُمرَّرة للمحاكي.
   عند اختلافهما تفشل **كل** دوال `get()` و`exists()` داخل القواعد بخطأ تقييم
   غامض (`DatastoreException: evaluation error`) بلا سبب واضح في السجل، بينما قد
   تنجح اختبارات أخرى في المجموعة نفسها. هذا وحده كلّف ساعات تشخيص.
2. **دوال المسار تُرجع Path فعلًا، لكن `get()` عليها يحتاج حرسة `exists()`.**
   `memberPath(storeId)` و`storePath(storeId)` تُرجعان مسارًا، و`memberDoc(storeId)`
   تحرسه بـ`exists()` قبل `get(...).data` وتُرجع `null` عند الغياب. بلا هذه الحرسة
   يتحول غياب المستند إلى خطأ تقييم يُبطل الطلب كله بدل رفضه رفضًا نظيفًا.
3. **كتل `let` مرفوضة** من مُصرِّف القواعد في هذا الإصدار؛ استخدم دوالًا وسيطة.
4. **المحاكي لا يفقد بياناته بين التشغيلات.** أي بقايا من جولة سابقة تُفسد
   النتائج (عضوية منشأة سابقًا تجعل اختبار «الاعتراض» ينجح خطأً). لذلك يستدعي
   الاختبار `env.clearFirestore()` قبل البذر.
5. **قاعدة افتراضية بالرفض** في نهاية الملف، مع حراس `exists()` على كل قراءة
   مستند، حتى لا يتحول غياب المستند إلى سماح ضمني.

## الإبلاغ عن ثغرة

لا تفتح issue عامة لثغرة أمنية. أرسل التفاصيل إلى بريد المشرف على المستودع
(ظاهر في سجل `git log`) مع: خطوات إعادة الإنتاج، وأقل صلاحية تكفي للاستغلال،
والأثر على البيانات. سنؤكد الاستلام خلال 72 ساعة ونعمل على إصلاح ونشر القواعد.

## ما لا يغطيه هذا الملف

- مفتاح توقيع أندرويد وكلمة سره خارج المستودع ولا يُرفعان إليه.
- مفاتيح VAPID الخاصة تُدار في Firestore ولا تُكتب في الشيفرة.
- `firebase.json` لا يحتوي أي سر؛ معرّف المشروع وحده ليس سرًا.
