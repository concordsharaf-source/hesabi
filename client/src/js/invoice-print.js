export function renderThermalInvoiceHtml({ invoice, customer = null, storeName, formatMoney, formatAmount, formatDateTime, escapeHtml, paymentLabel }) {
  const rows = invoice.items.map((item) => `<tr><td class="item-name">${escapeHtml(item.productName)}<small>${formatAmount(item.quantity)} ${escapeHtml(item.unit)} × ${formatMoney(item.unitPrice)}</small></td><td class="amount" dir="ltr">${formatMoney(item.total)}</td></tr>`).join("");
  const remaining = invoice.paymentType === "آجل" ? `<tr><th>المتبقي</th><td dir="ltr">${formatMoney(invoice.remainingAmount)}</td></tr>` : "";
  const cashierDetails = `<table class="info-table"><tbody><tr><th>الكاشير المنفذ</th><td>${escapeHtml(invoice.cashierName || "الأدمن")}</td></tr></tbody></table>`;
  const customerDetails = invoice.customerName ? `<section class="card"><h3>بيانات العميل</h3><table class="info-table"><tbody><tr><th>الاسم</th><td>${escapeHtml(invoice.customerName)}</td></tr>${customer?.phone ? `<tr><th>الهاتف</th><td dir="ltr">${escapeHtml(customer.phone)}</td></tr>` : ""}${customer?.address ? `<tr><th>العنوان</th><td>${escapeHtml(customer.address)}</td></tr>` : ""}</tbody></table></section>` : "";
  return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(invoice.invoiceNumber)}</title><style>@import url("https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700&display=swap");@page{size:80mm auto;margin:4mm}*{box-sizing:border-box}body{width:74mm;margin:0 auto;color:#111;font-family:"Cairo",Tahoma,Arial,sans-serif;font-size:11px;line-height:1.45;overflow-wrap:anywhere;word-break:normal}h1,h2,h3,p{margin:0;text-align:center}h1{font-size:17px;font-weight:700}h2{margin-top:3px;font-size:13px}h3{padding:4px;font-size:11px;background:#e9eee9;border-bottom:2px solid #111}.muted{margin-top:3px;color:#444;font-size:9px}.rule{border:0;border-top:2px solid #111;margin:7px 0}.card{margin:7px 0;border:2px solid #111}.info-table,.items-table,.summary-table{width:100%;table-layout:fixed;border-collapse:collapse}.info-table th{width:42%;background:#f0f2ef}.info-table th,.info-table td{padding:4px 5px;border:1.5px solid #111;vertical-align:top;overflow-wrap:anywhere}.info-table td{text-align:left;font-weight:600}.items-table{border:2px solid #111}.items-table th{background:#dfe6df;font-size:10px}.items-table th,.items-table td{padding:5px 4px;border:1.5px solid #111;vertical-align:top;overflow-wrap:anywhere}.items-table th:first-child,.items-table td:first-child{width:70%;text-align:right}.items-table th:last-child,.items-table td:last-child{width:30%;text-align:left}.item-name small{display:block;color:#444;font-size:9px;line-height:1.35}.amount{white-space:normal;direction:ltr}.summary-table{margin-top:7px;border:2px solid #111}.summary-table th,.summary-table td{padding:4px 5px;border:1.5px solid #111;overflow-wrap:anywhere}.summary-table th{width:58%;text-align:right;background:#f0f2ef}.summary-table td{text-align:left;font-weight:600}.summary-table .final th,.summary-table .final td{font-size:13px;font-weight:700;background:#d9e9dc}.footer{margin-top:9px;text-align:center;font-size:9px;color:#444}</style></head><body><h1>${escapeHtml(storeName || "حسابي")}</h1><h2>فاتورة بيع — ${escapeHtml(invoice.invoiceNumber)}</h2><p class="muted">تاريخ الإصدار: ${escapeHtml(formatDateTime(invoice.date))}</p>${cashierDetails}${customerDetails}<hr class="rule"><table class="items-table"><colgroup><col style="width:70%"><col style="width:30%"></colgroup><thead><tr><th>المنتج والكمية</th><th>قيمة السطر</th></tr></thead><tbody>${rows}</tbody></table><table class="summary-table"><tbody><tr><th>الإجمالي قبل الخصم</th><td dir="ltr">${formatMoney(invoice.subtotal)}</td></tr><tr><th>الخصم</th><td dir="ltr">${formatMoney(invoice.discount)}</td></tr>${Number(invoice.deliveryFee) > 0 ? `<tr><th>التوصيل — ${invoice.deliveryChargeType === "customer" ? "على العميل" : "على المحل"}</th><td dir="ltr">${formatMoney(invoice.deliveryFee)}</td></tr>` : ""}<tr class="final"><th>الإجمالي النهائي</th><td dir="ltr">${formatMoney(invoice.total)}</td></tr><tr><th>طريقة السداد</th><td>${escapeHtml(paymentLabel)}</td></tr><tr><th>حالة السداد</th><td>${escapeHtml(invoice.paymentStatus || (invoice.paymentType === "آجل" ? "آجل" : "مدفوعة"))}</td></tr><tr><th>المبلغ المدفوع</th><td dir="ltr">${formatMoney(invoice.paidAmount)}</td></tr>${remaining}</tbody></table><p class="footer">شكرًا لتعاملكم معنا</p></body></html>`;
}
