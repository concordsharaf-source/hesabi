# ملاحظات تحقق الواجهة

تأكدت المعاينة من فتح شاشة الإعداد الأولي باللغة العربية وباتجاه RTL، وظهور حقول اسم المتجر ونوع النشاط والعملة وزر الدخول مع الهوية البصرية. أُدخل اسم متجر اختبار وكان النموذج قابلاً للتفاعل.

بعد الحفظ، انتقل التطبيق إلى لوحة التحكم وعرض اسم المتجر ومؤشرات اليوم الفارغة والتنبيهات والإجراءات السريعة. فُتح نموذج إضافة المنتج كذلك، وظهرت فيه حقول الاسم والباركود والكود الداخلي والسعرين والكمية الافتتاحية والحد الأدنى والوحدة، بما يطابق نطاق المرحلة الأولى.

أُدخل منتج اختبار باسم «حليب» مع سعر شراء 100 وسعر بيع 150 وكمية افتتاحية 20 وحد أدنى 5. بعد الحفظ، حدّثت اللوحة عدد المنتجات إلى منتج واحد وقيمة المخزون إلى 2,000 ر.س، وظهرت رسالة تؤكد حفظ المنتج والحركة الافتتاحية.

في شاشة المبيعات ظهر منتج الاختبار بكمية متاحة قدرها 20 وسعر بيع 150 ر.س. أُضيف إلى السلة بنجاح، ثم ظهرت الكمية وخيارات الزيادة والإنقاص والحذف والإجمالي المبدئي بقيمة 150 ر.س.

فُتحت مراجعة الفاتورة وأظهرت المنتج وطريقة الدفع والمبلغ المدفوع والإجمالي النهائي. أُتم بيع الاختبار بنجاح، وانتقل التطبيق إلى الفواتير مع ظهور `INV-000001` ورسالة تؤكد حفظ الفاتورة وخصم المخزون.

أظهرت شاشة المخزون بعد البيع كمية «حليب» عند 19 حبة وقيمة مخزون 1,900 ر.س، ما يؤكد خصم وحدة واحدة. كما فُتح نموذج حركة التعديل وأظهر الكمية الحالية وحقل الكمية الجديدة وحقلًا إلزاميًا لسبب التعديل.

سُجل تعديل جردي من 19 إلى 15 حبة مع سبب «جرد نهاية اليوم». أعادت الشاشة حساب قيمة المخزون إلى 1,500 ر.س وأظهرت رسالة تأكيد تسجيل حركة تعديل المخزون، وهو ما يغطي مسار ADJUSTMENT.

بعد إعادة تحميل التطبيق، لم تظهر شاشة الإعداد من جديد. ظهرت لوحة التحكم باسم متجر الاختبار مع المنتج والفاتورة ومبيعات اليوم وقيمة المخزون المحدثة، ما يؤكد استمرار بيانات IndexedDB عبر إعادة الفتح.

أظهر فحص وقت التشغيل أن `manifest.json` متاح بنجاح بنوع محتوى JSON، وأن عامل الخدمة مسجّل ونشط على نطاق التطبيق، بما يدعم تشغيل التطبيق دون اتصال بعد أول تحميل.

في تحديث ماسح الباركود، ظهر داخل نموذج «إضافة منتج» حقل باركود مستقل باتجاه LTR وزر «مسح» واضح ونص إرشادي يشرح أن المستخدم يستطيع الإدخال اليدوي أو المسح بالكاميرا.

في بيئة المعاينة التي لا تدعم `BarcodeDetector`، ظهرت نافذة ماسح برسالة عربية صريحة تفيد بأن الماسح غير مدعوم وأن حقل الباركود في نموذج المنتج يبقى متاحًا للإدخال اليدوي. أُغلقت النافذة فعادت الشاشة إلى نموذج المنتج دون إغلاقه أو تعطل التطبيق.

أعيد فتح نموذج منتج جديد بعد تحديث منطق IndexedDB لبدء اختبار منع استخدام باركود موجود لمنتج آخر.

أُدخل اسم عربي صحيح «حليب كامل الدسم» مع باركود المنتج الموجود. ظهرت فورًا رسالة «هذا الباركود مستخدم بالفعل للمنتج: حليب»، وبقي نموذج المنتج مفتوحًا بعد محاولة الحفظ؛ وبذلك لم يُنشأ منتج مكرر ولم تُفقد المدخلات.

أظهرت لقطة هاتف بعرض 360 بكسل أن ورقة الإعداد كانت تتمدد بسبب قياس عرض داخلي مرن في تخطيط الشبكة. حُددت الورقة بعرض الحاوية وحد أقصى مستقل، وأضيفت قيود `min-width: 0` إلى الحاويات الشبكية بدل إخفاء التجاوز.

في شاشة المبيعات ظهر اسم المنتج العربي «حليب» بترتيب طبيعي داخل نتيجة البحث، مع بقاء الباركود والأرقام قابلة للعرض في واجهة RTL.

في قائمة المنتجات ظهر المنتج المحفوظ بخانة عربية طبيعية، مع إبقاء رقم الباركود مقروءًا كقيمة مستقلة، واستمر عرض الأسعار والكمية دون تأثر.

أُدخل باركود فريد لمحاكاة نتيجة مسح جديدة، فأظهر النموذج رسالة «الباركود متاح للحفظ» مع استمرار عرض اسم المنتج العربي بشكل طبيعي.

حُفظ منتج «ماء معدني» بالباركود الفريد وبكمية افتتاحية 12، ثم ظهر في قائمة المنتجات بجانب منتج الاختبار الأول مع رسالة تأكيد حفظ الحركة الافتتاحية.

بعد إصلاح الحالة الفارغة، استُخدم الإجراء نفسه `data-action="new-product"` واختُبر فتح نموذج «إضافة منتج» بنجاح؛ ولذلك أصبح زر الحالة الفارغة يفتح النموذج بدل إعادة تحميل صفحة المنتجات نفسها.

في المرحلة الثانية، فتح التطبيق بنجاح مع ظهور أقسام الموردين والمشتريات والمصروفات والتقارير. كما ظهرت الحالة الفارغة للموردين مع زر إضافة مورد متصل بالفعل نفسه.

أُدخلت بيانات مورد اختبار باسم «مورد الحليب المحلي» ورقم هاتف وعنوان وملاحظة في نموذج الموردين، تمهيدًا للتحقق من الحفظ الدائم.

حُفظ المورد بنجاح وظهر فورًا في قائمة الموردين. كما فُتح قسم المشتريات بالحالة الفارغة الصحيحة مع إمكانية إنشاء فاتورة شراء.

فُتح نموذج فاتورة شراء جديدة بنجاح، وظهر المورد المحفوظ ضمن القائمة وتم اختياره وربطه بالفاتورة.

نجح البحث العربي عن منتج «حليب» داخل فاتورة الشراء، ثم أُضيف كسطر شراء بكمية وسعر شراء وإجمالي محسوب تلقائيًا.

حُفظت فاتورة شراء بالرقم `PUR-000001` وبقيمة 100.00، ثم زادت كمية الحليب من 15 إلى 16 داخل شاشة المخزون وارتفعت قيمة المخزون إلى 1,900.00؛ ما يؤكد أن العملية كتبت في IndexedDB وليس في الواجهة فقط.

فُتحت تفاصيل فاتورة الشراء، وظهر بند الحليب والكمية الأصلية وزر «مرتجع شراء» المرتبط بالفاتورة نفسها.

فُتح نموذج مرتجع الشراء وحدد التطبيق الكمية المتاحة للإرجاع بحد أقصى يساوي كمية بند الفاتورة، ثم أُدخلت كمية اختبار صحيحة مقدارها 1.

حُفظ مرتجع الشراء، ثم عادت كمية الحليب من 16 إلى 15 وانخفضت قيمة المخزون من 1,900.00 إلى 1,800.00، ما يؤكد تنفيذ حركة `PURCHASE_RETURN` على البيانات المخزنة.

فُتحت فاتورة البيع السابقة وظهر زر «مرتجع بيع» مع تفاصيل البند المباع، ما يوفّر مسارًا مباشرًا لتسجيل المرتجع من الفاتورة نفسها.

فتح نموذج مرتجع البيع أظهر الكمية المتاحة للإرجاع دون تجاوز البند المباع، وأُدخلت كمية اختبار مقدارها 1 ضمن الحد الصحيح.

حُفظ مرتجع البيع بنجاح، ثم زادت كمية الحليب من 15 إلى 16 وارتفعت قيمة المخزون إلى 1,900.00؛ ما يؤكد تنفيذ حركة `SALE_RETURN` وحفظها محليًا.

فُتح قسم المصروفات بالحالة الفارغة الصحيحة، ثم ظهر نموذج إدخال مصروف يشمل المبلغ والفئة والتاريخ والوصف والملاحظات.

حُفظ مصروف اختبار بمبلغ 25.00، وظهر في القائمة مع إجمالي الفترة وعدد العمليات؛ وبذلك أصبح محفوظًا وجاهزًا للاحتساب في التقارير.

عرضت شاشة التقارير صافي المبيعات بعد مرتجع البيع، وإجمالي المصروفات وصافي الربح، كما عادت شاشة المخزون بقيمة وكمية الحليب المحدثتين بعد العمليات.

أظهر سجل حركة المخزون تسلسلًا زمنيًا واضحًا لحركات `INITIAL` و`SALE` و`ADJUSTMENT` و`PURCHASE` و`PURCHASE_RETURN` و`SALE_RETURN` مع الكمية قبل وبعد كل حركة.

فُتحت شاشة المبيعات وأُضيف منتج «ماء معدني» إلى سلة جديدة بقيمة 40.00؛ وذلك لاختبار حفظ تكلفة المنتج وقت البيع في فاتورة المرحلة الثانية.

حُفظت فاتورة جديدة بالرقم `INV-000002` لمنتج الماء المعدني بقيمة 40.00، مع خصم المخزون ضمن معاملة واحدة، وهي فاتورة تستخدم تكلفة المنتج المحفوظة في حساب الربح.

أُعيد فحص شاشة الإعداد بعرض هاتف 360 بكسل بعد تثبيت التموضع، وظهرت ورقة السجل كاملة داخل العرض مع الهوية والحقول والإجراء الأساسي دون تجاوز أفقي.

بعد ترقية IndexedDB إلى الإصدار الثالث، ظهر قسم العملاء بالحالة الفارغة الصحيحة مع زر إضافة عميل وإجمالي دين صفر، دون فقد بيانات المنتجات والمبيعات السابقة.

فُتح نموذج إضافة عميل وأُدخلت بيانات أحمد (الهاتف والعنوان) بنجاح قبل تثبيت الحفظ المحلي.

حُفظ العميل أحمد وظهر مباشرةً ضمن القائمة برصيد صفر، ثم فُتحت شاشة المبيعات مع بقاء المخزون والمنتجات السابقين ظاهرين.

ظهرت سلة البيع بعنصر كمية [-] [1] [+] قابل للكتابة، ثم عرضت مراجعة البيع خيارَي النقد والآجل وقائمة اختيار العميل وخلاصة المبلغ المتبقي.

تم تفعيل خيار البيع الآجل وتأكدت الواجهة من إظهار اختيار العميل، ثم اختير العميل أحمد لربط الفاتورة بحسابه.

أُدخل مبلغ مدفوع صفر لبيع آجِل بقيمة 40.00، فأظهرت الواجهة المتبقي 40.00 وحالة «غير مدفوعة»، ثم حُفظت الفاتورة INV-000003 مربوطة بالعميل أحمد.

أُجريت ترقية IndexedDB آمنة إلى الإصدار الرابع لإضافة فهرس العميل إلى المبيعات القائمة، وبقيت بيانات المتجر محفوظة. فتح حساب أحمد بعدها بنجاح وأظهر مبيعات 40.00 ومدفوعات 0.00 ورصيدًا مستحقًا 40.00 مع عملية بيع آجلة مسجلة.

في المحاولة الأولى لمبلغ 50.00 مقابل رصيد 40.00، كُشف أن عنصر الكمية كان يَسوي الإدخال اليدوي إلى الحد الأقصى قبل الحفظ، فحُفظت دفعة صحيحة بقيمة 40.00 بدل أن تظهر رسالة الرفض. عُدّل نموذج الدفعة ليُبقي القيمة المدخلة ويُحيل قرار الرفض إلى طبقة البيانات، التي تعيد الرسالة «المبلغ المدفوع أكبر من الرصيد المستحق». سيعاد الاختبار بعد إنشاء رصيد جديد.

بدأت فاتورة اختبارية جديدة لمنتج الماء المعدني بقيمة 40.00، مع بقاء المنتجات والمخزون المتاحين ظاهرين بعد إعادة تحميل التطبيق، تمهيدًا لإعادة اختبار البيع الآجل والتحصيل بعد الإصلاح.

اختير البيع الآجل، وربطت الفاتورة الجديدة بالعميل أحمد مع دفعة أولية صفر. حُفظت كفاتورة `INV-000004` بقيمة 40.00 وحالة «غير مدفوعة»، لتصبح رصيدًا جديدًا صالحًا لإعادة اختبار رفض الدفع الزائد والدفعات الجزئية.

أُدخلت دفعة 50.00 مقابل الرصيد المستحق 40.00 بعد الإصلاح، فبقي النموذج مفتوحًا وأظهر التطبيق الرسالة المطلوبة: «المبلغ المدفوع أكبر من الرصيد المستحق.» ولم تُنشأ دفعة جديدة أو يتغير الرصيد؛ وبذلك نجح اختبار منع الدفع الزائد.

حُفظت دفعة صحيحة بقيمة 30.00 من أحمد. أظهر إيصال الدفعة الرصيد قبل التحصيل 40.00 والرصيد بعده 10.00، وبذلك نجح اختبار الدفعة الجزئية الأولى دون احتسابها كمبيعات جديدة.

حُفظت الدفعة النهائية بقيمة 10.00. أكد إيصالها أن الرصيد قبل الدفع 10.00 وبعده 0.00، وبذلك نجح اختبار تسوية الدين بالكامل.

بعد إغلاق الإيصال، بقيت بيانات العميل محفوظة، وبدأت مراجعة فاتورة جديدة لمنتج الماء المعدني بقيمة 40.00 لإجراء اختبار مرتجع البيع الآجل.

حُفظت الفاتورة الآجلة `INV-000005` لعميل أحمد بقيمة 40.00 ودفعة أولية صفر، وهي الآن مرجع اختبار تخفيض دين العميل عبر مرتجع البيع.

سُجل مرتجع كامل لوحدة الماء من الفاتورة `INV-000005`. أكدت الواجهة حفظ المرتجع وزيادة المخزون، وتحولت حالة الفاتورة إلى «مدفوعة» لأن قيمة المرتجع خفّضت الرصيد المتبقي من 40.00 إلى صفر.

أظهرت قائمة العملاء بعد المرتجع إجمالي ديون 0.00 ورصيد أحمد 0.00، ما يؤكد أن المرتجع أثر في رصيد العميل فعليًا وليس في حالة الفاتورة فقط.

سُجلت حركة تعديل موثقة للحليب من 16 إلى 30 حبة مع سبب اختبار عناصر الكمية، وظهرت الكمية الجديدة وقيمة المخزون المحدثة في شاشة المخزون.

كُشف خلال اختبار السلة أن أول ضغطة على زر الإنقاص بعد إدخال 25 كانت تثبت الإدخال فقط؛ فصُحح تسلسل التحديث. بعد إعادة التحميل، كُتبت 25 يدويًا ثم خفّض زر «−» القيمة مباشرةً إلى 24 وأعاد زر «+» القيمة إلى 25، مع تحديث الإجمالي في كل خطوة.

في اختبار تجاوز المخزون، أُدخلت كمية 11 يدويًا لمنتج الماء المعدني مع مخزون متاح 9. عند تثبيت الإدخال، ظهرت الرسالة «الكمية المتوفرة غير كافية» وعادت كمية السلة الآمنة إلى 1؛ لذلك لم يكن ممكنًا تجاوز المخزون.

بعد إصلاحات الاختبار، نجح بناء الإنتاج عبر `pnpm build`، كما نجحت الاختبارات المنطقية الإحدى عشرة عبر `pnpm test` دون حالات فشل.

بعد إعادة فتح التطبيق، بقي متجر الاختبار والمنتجات والفواتير ودفعات العملاء محفوظة. ظهر أحمد برصيد صفر، وفتح حسابه عرض إجمالي مبيعات 120.00 وإجمالي مدفوع 80.00 وسجلًا محفوظًا للبيع الآجل والدفعات ومرتجع البيع؛ وبذلك نجح اختبار الثبات في IndexedDB.

فُحصت واجهة الهاتف بعروض 320 و360 و375 و390 و412 و430 بكسل. ظهرت ورقة الإعداد والحقول والأزرار كاملة ضمن كل عرض، من دون تمرير أفقي، وبقيت عناصر اللمس الأساسية واضحة وكبيرة.

في تحديث فواتير الشراء، ظهر المورد كحقل اختياري بخيار افتراضي «بدون مورد». أُضيف الماء المعدني وحُفظت الفاتورة `PUR-000002` بقيمة 25.00 دون مورد، وظهرت في القائمة بهذه التسمية مع تأكيد زيادة المخزون. كما ظهرت أسماء المنتجات العربية طبيعيًا في نتائج المبيعات وسلة البيع بعد تثبيت اتجاهها RTL صراحةً.

فُتح نموذج إضافة منتج بعد التعديل، فظهرت حقول الأسعار والكمية الافتتاحية والحد الأدنى فارغة بلا أصفار افتراضية. كما صار الخصم وحقول كميات المرتجعات تبدأ فارغة، بينما تبقى قيم الإجماليات للعرض فقط. نجح بعدها `pnpm build` و`pnpm test` بجميع الاختبارات الإحدى عشرة.

في الإصدار النهائي، رُقيت IndexedDB إلى الإصدار الخامس دون فقد بيانات المتجر، وأضافت مخازن دفعات الموردين وسجل معاملاتهم والصندوق والجرد. أُنشئت فاتورة شراء آجلة `PUR-000003` لمورد الحليب المحلي بقيمة 25.00؛ ظهرت مباشرةً كمستحق للمورد في حسابه. ثم سُجلت دفعة صحيحة 25.00 فظهر الإيصال برصيد قبل الدفع 25.00 وبعده 0.00.

سُجل إيداع صندوق يدوي بقيمة 100.00 مع سبب موثق، فارتفع رصيد الصندوق من 45.00 إلى 145.00 وظهر الإيداع في سجله. كما سُجل جرد للماء المعدني من 11 إلى 10 حبات بملاحظة «جرد نهاية اليوم»، فظهر فرق −1 وخُفضت قيمة المخزون. نجح تصدير نسخة احتياطية JSON وتحقق سجل تنزيلات المتصفح من الملف `hesabi-backup-2026-08-21.json`، مع وجود تحقق بنيوي وتحذير تأكيد قبل الاستعادة أو المسح.

فُعّل الوضع الداكن من زر شريط العنوان ثم أُعيد فتح التطبيق، فبقي التفضيل محفوظًا وظهر زر العودة للوضع الفاتح. كما نجح تصدير تقرير CSV، وأظهرت لقطات عروض 320 و375 و390 و412 و430 بكسل شاشة الإعداد كاملة بلا تمرير أفقي. في نهاية الاختبار، نجح `pnpm build` و`pnpm test` مع 12 اختبارًا منطقيًا ناجحًا، وأكد فحص IndexedDB الإصدار 5 وجود سجلات المورد والصندوق والجرد.

بعد توضيح قاعدة أن الآجل دين، كُشف أن بعض الفواتير الآجلة القديمة احتفظت برصيد متبقٍ بعد تسجيل الدفعة رغم أن رصيد العميل أو المورد الكلي أصبح صفرًا. رُقيت IndexedDB إلى الإصدار 6 بترحيل آمن لمطابقة الدفعات بالفواتير الآجلة زمنيًا، وتحديث `remainingAmount` و`paymentStatus` لكل فاتورة دون تغيير أرصدة الحسابات السليمة. أكد الفحص أن الفواتير الآجلة الثلاث للمبيعات وفاتورة الشراء الآجلة أصبحت جميعها «مدفوعة» برصيد متبقٍ صفر، مع بقاء ديون العملاء ومستحقات الموردين وصندوق النقد عند القيم الصحيحة.

استُبدلت قائمة التحصيل في مراجعة البيع بزري «كاش» و«تحويل» متساويين. ظهر كاش بلون أخضر عند اختياره، وانتقل التباين إلى أزرق عند اختيار تحويل. أكد فحص النموذج أن القيمة المخفية المحفوظة للفواتير أصبحت «تحويل» عند الضغط عليه، وأن عرض الزرين متساوٍ (149 بكسل لكل زر) دون فتح أو حفظ فاتورة اختبارية.

أضيفت نغمة نجاح محلية قصيرة في مسار ماسح الكاميرا: تُهيأ واجهة Web Audio عند فتح الماسح من نقرة المستخدم، ثم تصدر النغمة فور عثور `BarcodeDetector` على قيمة باركود صحيحة وقبل إغلاق جلسة الكاميرا. لم يُضف أي ملف صوت خارجي، ولا يؤثر غياب دعم الصوت أو الماسح في الإدخال اليدوي. نجح البناء والاختبارات المنطقية الاثنا عشر، وتأكد توفر Web Audio في بيئة المعاينة؛ أما `BarcodeDetector` فلم يكن مدعومًا في هذه البيئة، لذلك بقي اختبار التعرّف الفعلي مخصصًا لهاتف أو متصفح مدعوم.

بعد ترقية IndexedDB إلى الإصدار 7، ظهرت شاشة دخول محلية تضم حساب الأدمن الأولي `admin`. نجح الدخول بالرمز المؤقت ثم فُرضت شاشة تغيير الرمز قبل ظهور لوحة التحكم. من صفحة الحسابات أنشئ حساب كاشير تجريبي باسم مستخدم `cashier` وحالة نشطة، ثم سُجّل الخروج والدخول به. أظهرت الواجهة للكاشير زري التنقل «المبيعات» و«الفواتير» فقط، ولم تظهر له لوحة الأرباح أو الإحصائيات أو المنتجات أو المخزون أو الموردون أو التقارير أو الإعدادات. نجح البناء، ونجحت 15 حالة اختبار منطقي إضافة إلى اختبار Vitest للجلسة.

فُحصت نسخة Vercel على `https://hesab-tau.vercel.app/` بعد إصلاح PWA. عاد `manifest.json` بحالة 200 وبقيمة `display: standalone` ونطاق ومعرّف نسبيين صالحين، وعرّف أيقونتين بحجمي 192×192 و512×512. كما عاد `service-worker.js` بحالة 200 وكان عامل الخدمة نشطًا على نطاق Vercel نفسه. رُقيت تسمية الذاكرة إلى `hesabi-pwa-v2` كي يستبدل الهاتف الإصدار القديم عند التحديث.

اختُبرت الجلسة الدائمة على حساب الأدمن: بعد تسجيل الدخول أُعيد فتح التطبيق مباشرة فظهرت لوحة الإدارة دون طلب اسم مستخدم أو رمز دخول. بعد الضغط على «تسجيل الخروج» وإعادة فتح التطبيق، ظهرت شاشة الدخول كما يجب. لا تُحفظ كلمة المرور أو بصمتها في علامة الجلسة؛ تحفظ الهوية والصلاحية فقط، وتُستبعد العلامة من ملف النسخة الاحتياطية.

اختُبرت الجلسة الدائمة أيضًا لحساب الكاشير: بعد دخول `cashier` أُعيد فتح التطبيق فاستعاد شاشة المبيعات مباشرةً، وظهرت له المبيعات والفواتير فقط من دون أي قسم إداري أو مالي. ثم استُخدم تسجيل الخروج وأعاد التطبيق إلى شاشة الدخول، ما يؤكد مسح جلسة الكاشير يدويًا.

بعد نشر تحديث جلسة الدخول ظهر أن عامل الخدمة السابق يمكن أن يعيد HTML قديمًا يشير إلى ملف JavaScript حُذف مع إصدار Vercel الجديد. عُدّل العامل إلى `hesabi-pwa-v3` ليجلب صفحات التطبيق من الشبكة أولًا ويستخدم النسخة المخزنة عند انقطاع الإنترنت فقط. بعد وصول النشر، عاد `service-worker.js` بالإصدار v3، وتحقق تحميل `https://hesab-tau.vercel.app/` من شبكة اختبار نظيفة بنجاح دون صفحة فارغة.

على نسخة Vercel المنشورة، أُنشئ متجر اختبار محلي، وسُجّل الدخول بالأدمن ثم غُيّر الرمز الأولي. بعد إعادة فتح `https://hesab-tau.vercel.app/` استعيدت لوحة الأدمن مباشرةً دون طلب الدخول، ثم أعاد تسجيل الخروج النسخة إلى شاشة الدخول. وبذلك تحقّق استمرار الجلسة فعليًا على النطاق المنشور.

أُجري اختبار تكاملي معزول على IndexedDB للإصدار الحالي: أنشئ منتج تحقق، ثم فاتورة شراء ومرتجع شراء بقيمة 10، وفاتورة بيع ومرتجع بيع بقيمة 15. صُدرت نسخة JSON، ثم استعيدت داخل بيئة التحقق؛ فظهرت سجلات المرتجعين بعد الاستعادة كما هي، ولم تتضمن النسخة علامة الجلسة الدائمة. أعيدت بيانات التطوير الأصلية تلقائيًا بعد الاختبار. كما قاس فحص Chromium عروض CSS عند 320 و360 و375 و390 و412 و430 بكسل، وكانت قيمة `scrollWidth` مساوية لعرض كل نافذة في الحالات الست، دون تمرير أفقي.

بعد إعادة بيانات التطوير الأصلية، سُجل دخول الأدمن وظهرت لوحة المتجر باسم «بقالة الاختبار» مع منتجين فعّالين، وقيمة مخزون 3,250، ورصيد صندوق 145؛ ما يؤكد ثبات البيانات الأصلية بعد دورة اختبار المرتجعات والنسخ والاستعادة.

في تحديث النسخ السحابي المجاني، نُشرت قواعد Firestore المقيدة بصاحب `uid` فقط، وفُعلت مصادقة البريد/كلمة المرور، وأضيفت النطاقات المنشورة. ظهر للأدمن في الإعدادات قسم منفصل بعنوان «نسخ سحابي مجاني» يوضح أنه اختياري ويدوي ولا يبدل حسابات التطبيق المحلية. فُتح حوار الربط وتحقق بصريًا من حقلي البريد وكلمة المرور وخياري الدخول أو الإنشاء، دون إنشاء حساب Firebase تجريبي أو رفع بيانات متجر فعلية.

نجح `pnpm build` بعد إضافة Firebase Web SDK، ونجحت 23 حالة اختبار Node إضافة إلى اختبار Vitest. شملت الاختبارات الجديدة قراءة إعداد Firebase دون إنشاء بيانات، وتجزيء نسخة JSON وإعادة بنائها، ورفض جزء متلاعب به عبر بصمة SHA-256، ومنع النسخة فوق الحد التشغيلي، وحجب الكاشير عن الرفع والاستعادة السحابيين. أظهر فحص وحدة تحكم المتصفح بعد فتح الإعدادات وحوار الربط عدم وجود أخطاء. يبقى اختبار التكامل الخارجي—إنشاء أو دخول حساب Firebase حقيقي، رفع نسخة اختبار، عرضها واستعادتها—معلقًا إلى أن يوافق مالك المتجر على الحساب وبيانات الاختبار المستخدمين.

دُفعت النسخة إلى GitHub في الالتزامين `bfcd06c` و`dec4767`. بعد انتظار النشر التلقائي على Vercel، عاد ملف JavaScript الحي باسم `index-pERq0Uf9.js` وكان يحتوي نص «نسخ سحابي مجاني»، وهو تحقق أن حزمة واجهة النسخ الجديدة وصلت إلى `https://hesab-tau.vercel.app/`.

في تحديث التحويلات والفواتير، ظهرت بطاقة «تحويلات اليوم» في الرئيسية بقيمة صفر عند غياب تحويلات اليوم ونص يوضح الحالة. تؤكد قائمة الفواتير في بيانات الاختبار وسيلة السداد بعبارات «دين» و«كاش» بدل الاكتفاء بنوع الدفع الداخلي. فُتحت فاتورة `INV-000002`، فظهر فيها حقل «طريقة السداد: كاش» وأزرار «مشاركة» و«طباعة حرارية» و«مرتجع بيع» بوضوح. نجح البناء و24 اختبار Node إضافة إلى Vitest، كما مر فحص عروض 320 و360 و375 و390 و412 و430 بكسل دون تمرير أفقي. يغلف إخراج الطباعة الحرارية الفاتورة في نافذة مستقلة بعرض 80 مم، بينما تستخدم المشاركة واجهة الجهاز أو نسخ النص كبديل آمن.

تدعم دفعة العميل الآن اختيار «كاش» أو «تحويل» وتحفظ وسيلة التحصيل في سجل الدفعة؛ تدخل الدفعات المحولة في بطاقة «تحويلات اليوم»، بينما تبقى الدفعات الكاش وحدها ضمن رصيد الصندوق.

في معاينة التطوير، نُقر زر «مشاركة» لفاتورة `INV-000002` فظهر إشعار «تم نسخ الفاتورة للمشاركة»، ما تحقق من المسار البديل المحلي دون إرسال الفاتورة إلى أي تطبيق خارجي. كما استُدعي زر «طباعة حرارية» لتوليد نافذة الطباعة الخاصة بالفاتورة دون اختيار طابعة أو تنفيذ مهمة طباعة.

رُفع التحديث إلى GitHub في الالتزام `a4a2406`. بعد انتظار النشر التلقائي، ظهرت الحزمة الحية `index-G52SwZ6m.js` على `https://hesab-tau.vercel.app/` وكانت تتضمن نصوص «تحويلات اليوم» و«طباعة حرارية» و«طريقة التحصيل»، وهو تحقق وصول التحديث إلى Vercel.

أضيف اختبار آلي منفصل لقالب الطباعة الحرارية. أثبت الاختبار احتواء HTML على قاعدة `@page` بحجم `80mm`، ورقم الفاتورة والبند مع ترميز HTML آمن، والإجمالي وطريقة السداد والمبلغ المتبقي للفاتورة الآجلة. نجح البناء و`pnpm test` بعده بإجمالي 25 اختبار Node وVitest.

دُفع اختبار القالب إلى GitHub في الالتزام `aa8eec1`. أكدت الحزمة الحية الأحدث `index-zH412c07.js` على Vercel احتواءها على قاعدة `@page{size:80mm auto` ونصي «تحويلات اليوم» و«طباعة حرارية».

في معاينة التطوير بعد تحسين القراءة، ظهر الوضع الداكن بخلفيات داكنة متمايزة ونصوص رئيسية فاتحة وبيانات قابلة للقراءة داخل بطاقات اللوحة. بقيت الصفحة في التطبيق بعد تجربة زر الرجوع، ولم يحدث انتقال إلى صفحة سابقة أو إغلاق للواجهة.

عند فتح ماسح الباركود من المبيعات، ظهرت نافذة «مسح منتجات متواصل» مع التعليمات وزري «إعادة المحاولة» و«إنهاء المسح». في متصفح المعاينة الذي لا يدعم `BarcodeDetector`، بقي الإدخال اليدوي متاحًا؛ أما الأجهزة المدعومة فتستمر فيها الكاميرا بعد إضافة المنتج إلى السلة حتى يختار المستخدم «إنهاء المسح».

بعد تحويل حماية الرجوع إلى نمط تأكيد بنقرتين، جُربت الضغطة الأولى على زر الرجوع في المعاينة؛ بقيت لوحة التطبيق مفتوحة ولم يحدث انتقال خارجها. يتولى الاختبار المنطقي التحقق من أن الضغط الثاني ضمن مهلة التأكيد هو وحده الذي يسمح بالخروج المقصود.

لا يوفّر متصفح المعاينة `BarcodeDetector` أو كاميرا فعلية، لذلك لم يكن اختبار بث الكاميرا الحقيقي متاحًا في هذه البيئة. بديل التحقق المتاح هو اختبار وحدة يمنع تكرار الرمز في جلسة المسح، وفحص واجهة المسح وزر الإنهاء، مع إبقاء الإدخال اليدوي متاحًا عند عدم الدعم. يلزم اختبار هاتف فعلي أو متصفح يدعم `BarcodeDetector` للتحقق النهائي من أذونات الكاميرا والبث.

نُشر التحديث في GitHub بالالتزام `8f27016`. بعد انتظار النشر التلقائي، ظهرت الحزمة الحية `index-CrxJUKx3.js` على Vercel وفيها النصوص «مسح منتجات متواصل» و«إنهاء المسح» و«اضغط زر الرجوع مرة أخرى»، وهو تحقق وصول تحديث الماسح وحماية الرجوع إلى الرابط الحي.

بعد ملاحظة تكرار المنتج أثناء بقاء الباركود داخل الإطار على جهاز داعم، أضيف قفل للرمز المقروء لا يتحرر عند فقد كشف عابر؛ لا يعود الرمز مقبولًا إلا بعد غيابه المتصل عن الإطار لمدة 650 مللي ثانية ثم ظهوره مجددًا. نجح اختبار هذه المهلة ضمن 28 اختبارًا. نُشر الإصلاح في الالتزام `d90aec7`، وأكدت الحزمة الحية `index-CHinzsSB.js` وجود قيمة التأخير ونصوص جلسة المسح. يبقى التحقق الميداني من المستخدم على جهاز الكاميرا المطلوب لإغلاق الاختبار التشغيلي.

فُتح نموذج فاتورة شراء جديدة في معاينة التطوير بنجاح مع بقاء فاتورة الشراء الحالية دون حفظ أو تغيير للبيانات. سيُفحص سطر المنتج بحقوق العبوة في الخطوة التالية قبل تنفيذ أي شراء اختباري.

أُضيف منتج موجود إلى فاتورة شراء غير محفوظة؛ ظهر السطر بحقوق «نوع العبوة»، «عدد العبوات»، «حبات/عبوة»، «سعر العبوة»، «سعر الحبة»، و«سعر البيع للحبة»، وعرض قبل الحفظ عدد الحبات التي ستدخل المخزون. لم تُحفظ هذه الفاتورة التجريبية ولم تتغير بيانات المخزون.

أعيد فتح فاتورة الاختبار بعد إصلاح تفاعل حقول العبوة، وأضيف المنتج من جديد بنجاح. ما زال الاختبار في الواجهة فقط من دون حفظ أو تغيير بيانات المنتج أو المخزون.

في فاتورة شراء غير محفوظة، أُدخلت 24 حبة داخل عبوة سعرها 100؛ أظهر التطبيق «سيضاف 24 حبة إلى المخزون» وسعر حبة `4.17` وإجمالي `100.00` بعد تثبيت الحقل. يثبت ذلك أن إدخال العبوة يتحول إلى وحدات المخزون وأن سعر الحبة يشتق تلقائيًا، من دون حفظ أي فاتورة اختبار.

نجح اختبار IndexedDB معزول لشراء كرتونين، كل منهما 24 حبة وسعره 1,200. تحولت الكمية إلى 48 حبة، وأصبح سعر شراء الحبة 50 وسعر بيعها 65، وسجلت الفاتورة إجمالي 2,400 مع بيانات الكرتون. لم يستخدم الاختبار قاعدة بيانات المتصفح أو بيانات المتجر الفعلية. نجح البناء و30 اختبار Node إضافة إلى Vitest، كما مر فحص عروض الهاتف الستة دون تمرير أفقي.

نُشر تحديث العبوات وخيارات الباركود في الالتزام `387811c`. بعد انتظار النشر التلقائي، حملت Vercel الحزمة `index-B3ELEOxN.js` وكانت تحتوي عبارات «حبات/عبوة» و«سعر العبوة» و«بحث بالرمز» و«إعادة المسح»، وهو تحقق وصول التحديث إلى الرابط الحي.

في تحديث تأكيدات الجلسة، أدى حدث الرجوع المحاكى إلى نافذة «هل تريد الخروج من حسابي؟» مع خياري البقاء أو الخروج، وأغلق خيار البقاء النافذة وأبقى التطبيق مفتوحًا. كما فتح زر تسجيل الخروج نافذة تأكيد منفصلة ولم يمسح الجلسة قبل الموافقة. ظهر في فاتورة الشراء زر مسح الباركود، وعند عدم دعم القارئ ظهر إدخال الباركود يدويًا وإعادة المحاولة داخل سياق الشراء من دون حفظ فاتورة.

أضيف منتج موجود إلى فاتورة شراء تجريبية غير محفوظة. ظهرت تلقائيًا تسميات «عدد الكراتين»، و«حبة/كرتون»، و«سعر الكرتون» عند اختيار الكرتون، مع بقاء سعر الحبة وسعر البيع والإجمالي ظاهرين. لم تحفظ الفاتورة ولم تتغير بيانات المخزون أو المنتج خلال الفحص.

في معاينة لا تدعم قارئ الباركود، فتحت جلسة المسح المتواصل وظهرت رسالة عدم الدعم إلى جانب حقل «أدخل الباركود يدويًا» وزري «بحث» و«إعادة المحاولة»، مع بقاء سلة البيع بلا تعديل.

أُدخل باركود غير مسجل يدويًا (`999999999999`) ثم نُفذ البحث. ظهرت نافذة «المنتج غير موجود» وفيها حقل تعديل الباركود وزر «بحث بالرمز» وزر «إعادة المسح» وزر إنشاء المنتج، وبقيت سلة البيع دون أي منتج مضاف. يحقق ذلك مسار التعذر والرمز الجديد من دون إنهاء الجلسة أو فقد خيار المسح.

استُبدل اختبار نموذج «الضغطتين» القديم بحارس رجوع قابل للاختبار يميز بين فتح تأكيد الخروج، وإغلاق نافذة قائمة أولًا، والسماح بالخروج بعد موافقة المستخدم. في معاينة التطوير فُتحت نافذة «هل تريد الخروج من حسابي؟» عبر حدث الرجوع، ثم اختير «نعم، خروج»؛ أغلقت النافذة ونفذ التطبيق مسار السماح بالخروج المقصود بدل إعادة فتح التأكيد. لم يُنفذ هذا الفحص على كاميرا أو هاتف المستخدم، ولا يغير بيانات المتجر.

بعد التعديل، نجح `pnpm build` و31 اختبار Node واختبار Vitest. يشمل ذلك اختبارًا منفصلًا لمسار «نعم، خروج» يثبت تنفيذ الرجوع الفوري ثم الرجوع المؤجل بعد الموافقة. كما أكد فحص Chromium عدم وجود تمرير أفقي عند عروض 320 و360 و375 و390 و412 و430 بكسل. لا يزال اختبار بث الكاميرا ومنع التكرار على هاتف فعلي معلقًا لأن بيئة المعاينة لا تدعم الكاميرا أو `BarcodeDetector`.

في فاتورة بيع تجريبية غير محفوظة، أدى اختيار «آجل» إلى ضبط `المبلغ المدفوع` على `0` وتعطيل الحقل، وعرض كامل الإجمالي دينًا مع إلزام اختيار العميل. يؤكد اختبار IndexedDB المعزول أن طبقة البيانات نفسها تفرض الصفر حتى إن وصل إليها مبلغ من الواجهة، وأن تحصيلًا بتحويل يظهر ضمن التحويلات فقط ولا يرفع رصيد الصندوق النقدي.

فُتح كشف حساب العميل في المعاينة وظهرت أزرار «مشاركة PDF» و«طباعة الحساب». أنشأت المشاركة ملف `كشف-حساب-أحمد.pdf` في التنزيلات. كما فُتحت فاتورة محفوظة وزر «مشاركة PDF» أنشأ `INV-000001.pdf` من القالب الحراري نفسه. تحقق إنشاء PDF محليًا أيضًا من ملف اختبار بنوع `application/pdf`. ظهر في الصندوق شريط حوالات مستقل للوارد والصادر، وأظهرت إعادة إدخال «حليب» في بحث المبيعات الكلمة بالترتيب الصحيح ونتيجتها المطابقة. نجح `pnpm build` و33 اختبار Node وVitest وفحص عروض الهاتف الستة دون تمرير أفقي.

نُشر التحديث في GitHub بالالتزام `5217ec7`. بعد انتظار النشر التلقائي، حملت Vercel الحزمة `index-BoAVk6tb.js` وفيها النصوص «مشاركة PDF»، و«الحوالات خلال الفترة»، و«غير مدفوعة — يُسجل التحصيل لاحقًا»، و«تبديل المستخدمين». فتح الرابط العام صفحة الدخول بنجاح؛ وتبقى بيانات المتجر، بحسب تصميم IndexedDB، محلية على جهاز المستخدم ولا تظهر في نسخة متصفح جديدة.

في نموذج «إضافة منتج جديد» ضمن فاتورة شراء غير محفوظة، ظهرت أيقونة «مسح» ملاصقة لحقل الباركود وحقل «الباركود الداخلي» المستقل. لم يُحفظ المنتج التجريبي. كما ظهرت في المصروفات بطاقتان منفصلتان للمصروفات اليومية والشهرية؛ وبدّل نموذج الإضافة فئاته وشرح التوزيع تلقائيًا بين «أكل وشرب/مواصلات…» و«إيجار/كهرباء/ماء…» عند اختيار الدورية.

فُتح تقرير فعلي، وظهرت قائمة تصدير تضم CSV وPDF وDOC. نزّل الاختبار المحلي الملفين `hesabi-report-2026-08-23.pdf` و`hesabi-report-2026-08-23.doc` بنجاح. اختبرت الدالة المحاسبية أن مصروف فبراير 280 يوزع إلى 100 عبر عشرة أيام، وأن مصروف أبريل 300 مع مصروف يومي 25 يصبح إجمالي الفترة الجزئية 125 وإجمالي الشهر 325 بلا تكرار. نجح `pnpm build` و35 اختبار Node وVitest وفحص عروض الهاتف الستة دون تمرير أفقي.

نُشر تحديث الباركود والتقارير والمصروفات في GitHub بالالتزام `0b938ca`. حملت Vercel الحزمة `index-Bek5rmwP.js` بعد النشر التلقائي، وتحققت منها النصوص «الباركود الداخلي» و«CSV للجداول» و«مصروفات شهرية» و«يُوزع هذا المبلغ». فتح الرابط العام صفحة تسجيل الدخول بنجاح؛ وتظل بيانات المتجر محلية على جهاز المستخدم طبقًا لتصميم IndexedDB.

ظهر زر «بيع» العائم في أسفل يسار الرئيسية وفي صفحة المبيعات. من الرئيسية انتقل الضغط عليه إلى «بيع جديد» وفتح نافذة «مسح منتجات متواصل» مباشرة. في متصفح المعاينة غير الداعم ظهر الإدخال اليدوي وإعادة المحاولة وزر «إنهاء المسح»، ثم أُغلقت الجلسة من دون إضافة مبيعات. نجح `pnpm build` و35 اختبار Node وVitest وفحص عروض الهاتف 320–430 بكسل دون تمرير أفقي.

نُشر الاختصار في GitHub بالالتزام `1d82f9b`. حملت Vercel الحزمة `index-T2QCwkyu.js` وتحققت منها التعريفات `open-sales-scanner` و«فتح المبيعات ومسح الباركود» و«بيع ومسح باركود». فتح الرابط العام صفحة الدخول بنجاح، بينما يتطلب ظهور الزر تسجيل الدخول المحلي على الجهاز مثل بقية واجهة المتجر.

في نموذج «إضافة منتج جديد» من فاتورة شراء غير محفوظة، ظهرت تسميات الكرتون «عدد الكراتين / حبة/كرتون / سعر الكرتون». عند تبديل نوع العبوة إلى كيس تغيرت فورًا إلى «عدد الأكياس / حبة/كيس / سعر الكيس»، ثم أُغلق النموذجان دون حفظ بيانات. ظهرت أيقونة التحويلات تحت الصندوق في التنقل، وفتحت صفحة مستقلة تعرض الوارد والصادر والصافي مع فلتر التاريخ وفصل صريح عن الصندوق النقدي.

مررت بطاقات اللوحة القيم الفعلية لأرباح اليوم ورصيد الصندوق، كما تحمل بطاقات الصندوق والتقارير قيمها الفعلية مسبقًا. تحقق اختبار CSS مؤقت أُزيل فورًا أن لون محتوى بطاقة `is-negative` هو `rgb(255, 147, 140)`، أي أحمر واضح في الوضع الداكن. نجح `pnpm build` و35 اختبار Node وVitest وفحص عروض الهاتف الستة دون تمرير أفقي.

نُشر تحديث التحويلات والتسميات والألوان السالبة في GitHub بالالتزام `8005187`. حملت Vercel الحزمة `index-c2b6MLF2.js` وتحققت منها النصوص «تحويلات واردة» و«صافي التحويلات» و«عدد الكراتين» و«عدد الأكياس». فتح الرابط العام صفحة تسجيل الدخول بنجاح؛ وتظهر تفاصيل المتجر بعد الدخول المحلي على جهاز المستخدم.

ظهرت في الرئيسية بطاقة «الداخل للصندوق» بقيمة نقد اليوم، بدل بطاقة الرصيد التراكمي. فُتحت المصروفات وكان تاريخ البدء الافتراضي `2026-08-01` والنطاق معرّف بوضوح بأنه الشهر الحالي مع بقاء حقلي التاريخ قابلين للتعديل. في المبيعات، فُتح الماسح في متصفح غير داعم وظهرت رسالة «يمكنك البحث بالكود الداخلي» وحقل «أدخل الكود الداخلي» وزر البحث المطابق، ثم أُغلقت الجلسة بلا بيع.

أضيف اختبار IndexedDB يعثر على المنتج بالكود الداخلي دون باركود، واختبار لوحة التحكم الذي يثبت أن الداخل النقدي اليومي يساوي مبيعات الكاش ولا يعتمد على الرصيد التراكمي. نجح `pnpm build` و37 اختبار Node واختبار Vitest وفحص عروض الهاتف عند 320 و360 و375 و390 و412 و430 بكسل دون تمرير أفقي.

نُشر التحديث في GitHub بالالتزام `23568a1`. حملت Vercel الحزمة `index-BdR-fnh_.js` وتحققت منها النصوص «بحث بالكود الداخلي» و«الداخل للصندوق» و«نقد وارد اليوم فقط» و«النطاق الافتراضي: الشهر الحالي». فتح الرابط العام صفحة تسجيل الدخول بنجاح؛ وتظهر بيانات المتجر المحلية بعد الدخول على جهاز المستخدم.

عند مشاركة `INV-000001` قبل الإصلاح، كان ملف PDF الناتج صفحة بيضاء رغم ظهور القالب المؤقت. استُبدل مسار التوليد برسم صريح للقالب ثم إدخاله في PDF، بعد انتظار اكتمال التخطيط. نُزّل الملف الجديد `INV-000001 (2).pdf` بحجم 45 كيلوبايت؛ وعند تحويله إلى صورة ظهرت صفحة حرارية تحتوي اسم المتجر ورقم الفاتورة والبند والإجماليات وطريقة السداد والمدفوع، بدل صفحة فارغة. نجح `pnpm build` و37 اختبار Node وVitest بعد الإصلاح.

نُشر إصلاح PDF في GitHub بالالتزام `3bf5cf7`. حملت Vercel الحزمة `index-BpDQZ4Nb.js` وتحققت منها وحدات `html2canvas` و`jspdf` ومسار «تعذر رسم محتوى الفاتورة لملف PDF» الوقائي. فتح الرابط العام صفحة الدخول بنجاح؛ ويبقى اختبار المشاركة على جهاز المستخدم مطلوبًا للتأكد من سلوك تطبيق المشاركة الأصلي هناك.

اختبرت فاتورة آجل مرتبطة بالعميل أحمد. أظهر PDF الناتج بطاقة مستقلة بعنوان «بيانات العميل» تضم الاسم أحمد والهاتف `777123456` والعنوان صنعاء، قبل بنود الفاتورة وإجمالياتها. نُزّل الملف `INV-000005.pdf` بحجم 58 كيلوبايت وحُوّل إلى صورة للتحقق البصري من البطاقة. كما تحقق اختبار القالب آليًا من عنوان القسم والاسم والهاتف والعنوان. نجح `pnpm build` و37 اختبار Node وVitest وفحص عروض الهاتف الستة.

بعد الإبلاغ عن تداخل العربية في PDF، استُبدل رسم HTML الخاص بمشاركة الفاتورة برسم حراري عالي الدقة يُنفَّذ مباشرة بمحرك النص العربي في المتصفح ثم يُضمَّن في ملف PDF. اختبرت فاتورة آجل فعلية للعميل أحمد بعد الإصلاح: ظهر اسم المتجر والعنوان «بيانات العميل» والاسم والهاتف والعنوان وبند «ماء معدني» والإجماليات وطريقة السداد كاملة، بحروف عربية متصلة وواضحة دون تراكب أو تبدل. نُزّل الملف `INV-000005 (8).pdf` وحُوِّل إلى صورة للتحقق البصري. نجح `pnpm build` و38 اختبار Node وVitest، مع استمرار نجاح فحص عروض الهاتف.

نُشر الإصلاح في GitHub عبر الالتزامين `f5b04e2` و`eca86f2`. تحققت الحزمة الحية `index-J3hiI8Ir.js` على Vercel من وجود مسار الرسم الحراري العربي ورابط الخط الدائم. كما حمّل Chromium الخط `HesabiArabicPdf` من الرابط المطلق بنجاح (`status: loaded`) قبل إنشاء PDF؛ وبذلك لا يعتمد إخراج الفاتورة على خط مثبت محليًا في جهاز المستخدم.

في تحسين القراءة، كُبرت أسماء المنتجات وبياناتها وأسعارها وكمياتها في قائمة المنتجات، وكُبرت أسماء وكميات وأسعار بطاقات المخزون وملخصه. تحققت الواجهة من عرض بطاقة «المنتجات» بقيمة `2` وبطاقة «فواتير اليوم» بقيمة `0` بأرقام إنجليزية. أضيف حقل بحث LTR برقم الفاتورة في صفحة الفواتير؛ وعند إدخال `INV-000003` ظهرت فاتورة واحدة مطابقة فقط. نجح `pnpm build` و39 اختبار Node وVitest، كما مر فحص عروض 320 و360 و375 و390 و412 و430 بكسل بلا تمرير أفقي.

عند استمرار البلاغ من جهاز المستخدم، عُزز مسار PDF بحيث يجلب سجل العميل مباشرة من IndexedDB عند المشاركة إذا لم يكن محمّلًا في حالة الواجهة، ويستخدم الاسم المخزن في سجل العميل عند الحاجة. كما عُرّف الخط العربي المضمّن بجميع أوزانه لضمان عدم الرجوع إلى خط بديل أثناء رسم الإيصال. رُقي عامل الخدمة إلى `hesabi-pwa-v4` لإجبار الأجهزة المثبتة على الحصول على الحزمة الجديدة. حُولت فاتورة العميل `INV-000005 (9).pdf` إلى صورة: ظهرت بطاقة رمادية محاطة بإطار بعنوان «بيانات العميل» وتضم أحمد و`777123456` وصنعاء، ثم ظهرت بنود الفاتورة والإجماليات بحروف عربية متصلة وواضحة. نجح `pnpm build` و39 اختبار Node وVitest وفحص العروض الهاتفية الستة.

عولج مسار كشف حساب العميل الذي كان يولد PDF أبيض أو غير مستقر بصريًا: أصبحت المشاركة ترسم كشف الحساب مباشرةً بمحرك النص العربي في المتصفح، وتضمّن بطاقة كبيرة مؤطرة بعنوان «بيانات العميل» تعرض الاسم والهاتف والعنوان. اختبرت `كشف-حساب-أحمد (2).pdf` بصريًا: ظهر أحمد و`777123456` وصنعاء داخل البطاقة بوضوح، مع ملخص الحساب وسجل العمليات في صفحة A4 واحدة من دون حروف متداخلة. كما ضُبطت إجراءات نوافذ الفاتورة وحساب العميل لتلتف ضمن أعمدة مرنة على الهاتف، فلا تتجاوز أزرار المشاركة والطباعة أو الأيقونات حدود الصفحة. رُقي عامل الخدمة إلى `hesabi-pwa-v5` لإيصال التصحيح للتطبيق المثبّت. نجح `pnpm build` و40 اختبار Node وVitest وفحص عروض الهاتف الستة.

كُبرت خطوط كشف الحساب PDF في بطاقة العميل، وبطاقات الملخص، وعنوان الجدول وصفوف العمليات مع زيادة المسافات الرأسية بصورة متوازنة. فُحص الملف `كشف-حساب-أحمد (3).pdf` بصريًا؛ بقي ضمن صفحة A4 واحدة وظهرت جميع الحروف العربية متصلة وواضحة، مع بطاقة العميل والملخص والعمليات دون قص أو تراكب. نجح `pnpm build` و40 اختبار Node وVitest وفحص عروض الهاتف الستة.

عولج خيار «البقاء في التطبيق» في نافذة تأكيد الخروج. كان الرابط العام للإغلاق يربط أول عنصر يحمل `data-dialog-close` فقط، وهو زر الإغلاق العلوي، لذلك لم يكن زر البقاء ينفذ الإغلاق. أصبح الربط يمر على جميع عناصر الإغلاق داخل النافذة، فيغلق خيار البقاء النافذة فورًا مثل زر × ومن دون محاولة رجوع إضافية. أضيف اختبار حماية صريح لهذا الربط؛ نجح `pnpm build` و41 اختبار Node وVitest وفحص عروض الهاتف الستة.

عولج الخروج المباشر من أول صفحة بعد فتح التطبيق. أصبح حارس الرجوع يستبدل حالة الصفحة الحالية بحالة حماية ثم يضيف نقطة حماية ثانية فور اكتمال فتح التطبيق، بدل إضافة نقطة واحدة فقط. لذلك يلتقط أول ضغط رجوع حتى من شاشة البداية ويعرض نافذة التأكيد، بينما يبقى مسار «نعم، خروج» قادرًا على مغادرة التطبيق المقصودة. أضيف اختبار يحاكي إنشاء نقطتي التاريخ من الحالة الأولى؛ نجح `pnpm build` و42 اختبار Node وVitest وفحص عروض الهاتف الستة.

أضيفت أيقونة اتصال خضراء بجوار إجراءات بطاقة العميل أو المورد عند توفر رقم هاتف محفوظ. يُنظّف الرابط الرقم إلى أرقام فقط مع الاحتفاظ بعلامة `+` الدولية عند وجودها، ثم يفتح `tel:` في تطبيق الاتصال على الجهاز. اختبرت الواجهة محليًا: ظهرت للأحمد ولـ«مورد الحليب المحلي» ذوي الرقم `777123456`، وربط كل زر إلى `tel:777123456`. لا ينشأ الزر عند غياب رقم هاتف. نجح `pnpm build` و43 اختبار Node وVitest وفحص عروض الهاتف الستة دون تمرير أفقي.

أضيف إلى حقل الهاتف في نموذجي العميل والمورد اختيار «جهات الاتصال» على الأجهزة التي تدعم Contact Picker API. عند اختيار جهة اتصال، يملأ التطبيق رقمها تلقائيًا ويملأ الاسم إذا كان الحقل فارغًا، مع إمكانية كتابة الرقم يدويًا دائمًا. تحققت بيئة المعاينة من المسار غير المدعوم: بقي حقل الهاتف قابلًا للإدخال وظهرت رسالة واضحة بأن الإدخال اليدوي متاح. يتحقق الاختبار من استدعاء `navigator.contacts.select` بالحقول `name` و`tel` للعميل والمورد. نجح `pnpm build` و44 اختبار Node وVitest وفحص عروض الهاتف الستة. يلزم اختبار المستخدم على هاتف Android داعم للتحقق النهائي من نافذة جهات الاتصال الأصلية.

بُسّط عرض المبالغ في الدالة المالية المركزية: تعرض القيم الصحيحة من دون أصفار عشرية، مثل `3000` بدل `3000.00`، وتظل الكسور الحقيقية حتى منزلتين عند وجودها. لا يغير ذلك أي مبلغ محفوظ أو عملية حسابية؛ إنه عرض فقط، وينعكس في البطاقات والفواتير والتقارير والطباعة وPDF عبر الدالة نفسها. فحصت الرئيسية بصريًا: ظهرت قيمة المخزون `3,250 ر.س` والداخل للصندوق `145 ر.س` والمبالغ الصفرية `0 ر.س` بلا `.00`. نجح `pnpm build` و45 اختبار Node وVitest وفحص عروض الهاتف الستة.

عولج تقرير PDF الذي كان يستخدم مرحلة HTML تتأثر بعرض العربية داخل بعض قارئات PDF. أصبح التصدير يرسم تقرير A4 مباشرةً بمحرك النص العربي في المتصفح، بخط مضمّن واتجاه RTL، ثم يضمّن الرسم في PDF. اختبرت الملف `hesabi-report-2026-08-23 (1).pdf` بصريًا: ظهر العنوان وفترة التقرير ورأسا الجدول وبنود المبيعات والمشتريات والمصروفات والأرباح والمبالغ بحروف عربية متصلة وواضحة بلا تداخل أو تبدل. نجح `pnpm build` و45 اختبار Node وVitest وفحص عروض الهاتف الستة.
